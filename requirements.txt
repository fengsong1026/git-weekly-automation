# git-report Python dependencies
#
# Install with: pip install -r requirements.txt

# openai Python SDK (optional — the report generator falls back to stdlib urllib)
# Uncomment if you want to use the SDK instead of the HTTP fallback:
# openai>=1.0.0

# Note: All other dependencies are from the Python standard library:
#   json, argparse, datetime, pathlib, collections, plistlib, subprocess, urllib
