# git-weekly-automation — No pip dependencies required.
#
# All scripts use only the Python 3.8+ standard library:
#   json, argparse, datetime, pathlib, subprocess, urllib, plistlib, fcntl
#
# Optional: install the openai SDK for better API error handling and retry logic.
# The script falls back to stdlib urllib if the SDK is not available.
#
#   pip install openai>=1.0.0
